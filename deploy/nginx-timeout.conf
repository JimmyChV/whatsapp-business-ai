# Copiar estas directivas al bloque location
# de /etc/nginx/sites-available/wa.lavitat.pe
# en el servidor Hetzner

proxy_read_timeout 120s;
proxy_connect_timeout 10s;
proxy_send_timeout 120s;

# Luego ejecutar: nginx -t && systemctl reload nginx
