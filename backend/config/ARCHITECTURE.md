# Global Backend Architecture Baseline (Phase 2)

Incremental migration scope:
1) keep server bootstrap stable
2) route registrars by domain
3) domain service wrappers
4) later: repositories + validators + controllers split
