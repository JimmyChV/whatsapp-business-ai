export function buildOperationPageProps(input = {}) {
  return {
    forceOperationLaunch: input.forceOperationLaunch,
    socket: input.socket,
    fileInputRef: input.fileInputRef,
    handleFileChange: input.handleFileChange,
    chats: input.chats,
    chatsLoaded: input.chatsLoaded,
    activeChatId: input.activeChatId,
    handleChatSelect: input.handleChatSelect,
    myProfile: input.myProfile,
    businessData: input.businessData,
    handleLogoutWhatsapp: input.handleLogoutWhatsapp,
    handleRefreshChats: input.handleRefreshChats,
    handleStartNewChat: input.handleStartNewChat,
    labelDefinitions: input.labelDefinitions,
    handleCreateLabel: input.handleCreateLabel,
    handleLoadMoreChats: input.handleLoadMoreChats,
    chatsHasMore: input.chatsHasMore,
    isLoadingMoreChats: input.isLoadingMoreChats,
    chatsTotal: input.chatsTotal,
    chatSearchQuery: input.chatSearchQuery,
    handleChatSearchChange: input.handleChatSearchChange,
    chatFilters: input.chatFilters,
    handleChatFiltersChange: input.handleChatFiltersChange,
    handleOpenCompanyProfile: input.handleOpenCompanyProfile,
    saasAuthEnabled: input.saasAuthEnabled,
    availableTenantOptions: input.availableTenantOptions,
    tenantScopeId: input.tenantScopeId,
    tenantSwitchError: input.tenantSwitchError,
    handleSaasLogout: input.handleSaasLogout,
    canManageSaas: input.canManageSaas,
    handleOpenSaasAdminWorkspace: input.handleOpenSaasAdminWorkspace,
    availableWaModules: input.availableWaModules,
    clientContact: input.clientContact,
    messages: input.messages,
    messagesEndRef: input.messagesEndRef,
    isDragOver: input.isDragOver,
    handleDragOver: input.handleDragOver,
    handleDragLeave: input.handleDragLeave,
    handleDrop: input.handleDrop,
    showClientProfile: input.showClientProfile,
    setShowClientProfile: input.setShowClientProfile,
    inputText: input.inputText,
    setInputText: input.setInputText,
    handleSendMessage: input.handleSendMessage,
    attachment: input.attachment,
    attachmentPreview: input.attachmentPreview,
    removeAttachment: input.removeAttachment,
    isAiLoading: input.isAiLoading,
    requestAiSuggestion: input.requestAiSuggestion,
    aiPrompt: input.aiPrompt,
    setAiPrompt: input.setAiPrompt,
    isCopilotMode: input.isCopilotMode,
    setIsCopilotMode: input.setIsCopilotMode,
    handleToggleChatLabel: input.handleToggleChatLabel,
    handleToggleChatPinned: input.handleToggleChatPinned,
    handleEditMessage: input.handleEditMessage,
    waCapabilities: input.waCapabilities,
    handleReplyMessage: input.handleReplyMessage,
    handleForwardMessage: input.handleForwardMessage,
    handleDeleteMessage: input.handleDeleteMessage,
    quickReplies: input.quickReplies,
    chatAssignmentState: input.chatAssignmentState,
    handleSendQuickReply: input.handleSendQuickReply,
    quickReplyDraft: input.quickReplyDraft,
    setQuickReplyDraft: input.setQuickReplyDraft,
    handleLoadOrderToCart: input.handleLoadOrderToCart,
    handleCancelEditMessage: input.handleCancelEditMessage,
    handleCancelReplyMessage: input.handleCancelReplyMessage,
    editingMessage: input.editingMessage,
    replyingMessage: input.replyingMessage,
    buildApiHeaders: input.buildApiHeaders,
    clientProfilePanelRef: input.clientProfilePanelRef,
    toasts: input.toasts,
    setToasts: input.setToasts,
    pendingOrderCartLoad: input.pendingOrderCartLoad,
    openCompanyProfileToken: input.openCompanyProfileToken,
    selectedCatalogModuleId: input.selectedCatalogModuleId,
    activeCatalogId: input.activeCatalogId,
    selectedWaModule: input.selectedWaModule,
    handleSelectCatalogModule: input.handleSelectCatalogModule,
    handleSelectCatalog: input.handleSelectCatalog,
    handleUploadCatalogImage: input.handleUploadCatalogImage,
    handleCartSnapshotChange: input.handleCartSnapshotChange,
    newChatDialog: input.newChatDialog,
    setNewChatDialog: input.setNewChatDialog,
    newChatAvailableModules: input.newChatAvailableModules,
    handleConfirmNewChat: input.handleConfirmNewChat,
    handleCancelNewChatDialog: input.handleCancelNewChatDialog,
    showSaasAdminPanel: input.showSaasAdminPanel,
    setShowSaasAdminPanel: input.setShowSaasAdminPanel,
    handleOpenWhatsAppOperation: input.handleOpenWhatsAppOperation,
    saasUserRole: input.saasUserRole,
    saasSession: input.saasSession,
    requestedWaTenantFromUrl: input.requestedWaTenantFromUrl,
    requestedLaunchSource: input.requestedLaunchSource,
    requestedWaSectionFromUrl: input.requestedWaSectionFromUrl,
    SaasPanelComponent: input.SaasPanelComponent
  };
}
